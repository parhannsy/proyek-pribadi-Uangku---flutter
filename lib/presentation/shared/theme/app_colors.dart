// lib/presentation/shared/theme/app_colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Dasar
  static const Color primaryBackground = Color(0xFF121212); // Dark Charcoal
  static const Color surfaceColor = Color(0xFF1E1E1E);      // Deep Slate Gray

  // Aksen
  static const Color accentGold = Color(0xFFFFC72C);       // Polished Gold
  static const Color negativeRed = Color(0xFFCF6679);     // Deep Burgundy (Pengeluaran)
  static const Color positiveGreen = Color(0xFF03DAC5);    // Emerald Green (Pemasukan/Netral)

  // Teks
  static const Color textPrimary = Color(0xFFE0E0E0);      // Light Silver
  static const Color textSecondary = Color(0xFFA0A0A0);    // Soft Gray
}